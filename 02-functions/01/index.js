//declare a function with name greet
function greet(name) {
  return `Hello, ${name}`;
}
//This function takes in a name parameter and
//returns a string that concatenates the name with the greeting message.
console.log(greet("Sidra"));
console.log(greet("Abrish"));
